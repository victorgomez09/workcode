spring:
    application:
        name: auth-service
    
    cloud:
        config:
            enabled: true
            uri: http://localhost:8081
            name: auth-service
    
jwt:
    secret: workcode_secret

admin-paths:
    paths:
        -
            uri: "/users"
            method: "POST"